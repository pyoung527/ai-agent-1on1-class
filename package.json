{
  "name": "ai-agent-1on1-class",
  "version": "1.0.0",
  "private": true,
  "description": "개인 대상 AI 에이전트 활용 교육용 HTML 자료",
  "scripts": {
    "start": "python3 -m http.server 4390 --bind 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check worksheet.js"
  }
}
